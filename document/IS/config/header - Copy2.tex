% === header.tex ===
% REVISED: Based on CU thesis formatting guidelines (บัณฑิตวิทยาลัย จุฬาฯ)

\usepackage{graphicx}
\usepackage{eso-pic}
\usepackage{nopageno}  % optional, removes page numbers
\usepackage{fontspec}  % for custom fonts (XeLaTeX)
\usepackage{polyglossia}  % language support
\usepackage{ucharclasses}
\usepackage{fancyhdr}  % for custom headers/footers



% CHANGED: Margins updated to CU thesis standard
%   - top=1.5in (3.81cm), bottom=1in (2.54cm)
%   - left=1.5in (3.81cm) for binding, right=1in (2.54cm)
% OLD: [a4paper, top=2.5cm, bottom=2.5cm, left=3cm, right=3cm]
\usepackage[letterpaper, top=2.54cm, bottom=2.54cm, left=3.54cm, right=2.54cm]{geometry}

% CHANGED: setspace for explicit line spacing control
% REVISED rev04: Changed from \singlespacing to \onehalfspacing (1.5 line multiple)
\usepackage{setspace}
\onehalfspacing

% ADDED: tikz for watermark opacity control (replaces transparent package for XeLaTeX compatibility)
\usepackage{tikz}

% ADDED rev04: pdflscape for landscape pages (chapter 1 tables)
\usepackage{pdflscape}

% ADDED rev04: booktabs for better table formatting
\usepackage{booktabs}

% ADDED rev04: array for column width control in tables
\usepackage{array}

% ADDED: titlesec for chapter/section title formatting per CU standard
\usepackage{titlesec}

% % --- 1️⃣ NEW: Reset section/subsection numbering per chapter ---
% \usepackage{chngcntr}
% \counterwithin*{section}{chapter}      % sections restart with each chapter
% \counterwithin*{subsection}{section}   % subsections restart with each section

% % --- 2️⃣ NEW: Set numbering and TOC depth ---
% \setcounter{secnumdepth}{3}   % number sections, subsections, subsubsections
% \setcounter{tocdepth}{3}      % include sections, subsections, subsubsections in TOC

% --- Languages ---
\setmainlanguage{english}
\setotherlanguage{thai}

% --- Fonts ---
% CHANGED: English font switched from Times New Roman to TH Sarabun New
%   - CU thesis requires TH Sarabun New for all text (Thai and Latin characters)
% OLD: \newfontfamily\englishfont{Times New Roman}[Scale=1.0]

\newfontfamily\englishfont{TH Sarabun New}
\newfontfamily\thaifont[Script=Thai]{TH Sarabun New}

% CHANGED: Thai font Scale from 1.2 to 1.0 (same font for both → unified scaling)
% OLD: \newfontfamily\thaifont[Script=Thai,Scale=1.2]{TH Sarabun New}

% --- Set global font size for document ---
\newcommand{\setdocfontsize}[1]{%
    \fontsize{#1}{1.2\baselineskip}\selectfont
}


% Body text = 16pt per CU standard (unchanged)
\setdocfontsize{16pt}

% --- Automatic font switching ---
\setDefaultTransitions{\englishfont}{\englishfont} % default font
\setTransitionTo{Thai}{\thaifont}
\setTransitionFrom{Thai}{\englishfont}

% --- Enable Thai line breaking ---
\XeTeXlinebreaklocale "th_TH"
\XeTeXlinebreakskip = 0pt plus 1pt

% REVISED rev04: Paragraph indent = hanging 0.01cm per prompt specification
%   - left: -0.01ch, special: hanging, by: 0.01cm
\setlength{\parindent}{0.01cm}

% ADDED rev04: Paragraph spacing — before: 0pt, after: 10pt
\setlength{\parskip}{10pt plus 1pt minus 1pt}

% ADDED rev04: Widow/orphan control
\widowpenalty=10000
\clubpenalty=10000

% ADDED: Chapter title formatting per CU standard
%   - Chapter title: 18pt Bold, centered
%   - Section title: 16pt Bold, left-aligned
%   - Subsection title: 16pt Bold, left-aligned
\titleformat{\chapter}[display]
  {\centering\bfseries\fontsize{18pt}{21.6pt}\selectfont}
  {\chaptertitlename\ \thechapter}{0.5em}{}
\titleformat{\section}
  {\bfseries\fontsize{16pt}{19.2pt}\selectfont}
  {\thesection}{1em}{}
\titleformat{\subsection}
  {\bfseries\fontsize{16pt}{19.2pt}\selectfont}
  {\thesubsection}{1em}{}

% --- Watermark background image ---
% CHANGED: Uses tikz opacity (0.25) for 75% transparency
%   - CU standard requires 70-80% transparent watermark
% OLD: \includegraphics[scale=1.0]{../config/cu_logo.jpg}
% \AddToShipoutPictureBG{%
%   \AtPageCenter{%
%     \begin{tikzpicture}[remember picture, overlay]%
%       \node[opacity=0.6] at (0,0) {\includegraphics{../config/cu_logo.jpg}};%
%     \end{tikzpicture}%
%   }%
% }
\AddToShipoutPictureBG{%
\begin{tikzpicture}[remember picture,overlay]
\node[opacity=0.25] at (current page.center)
{\includegraphics[width=0.55\paperwidth]{../config/cu_logo.jpg}};
\end{tikzpicture}%
}

% --- Fancy header setup ---
\pagestyle{fancy}
\fancyhf{}                % clear all header/footer fields
\fancyhead[R]{\thepage}   % page number top right
\renewcommand{\headrulewidth}{0pt}  % optional: remove header line
\renewcommand{\footrulewidth}{0pt}  % optional: remove footer line



% --- 1️⃣ COVER PDF PAGE NUMBERING (a,b,c,...) ---
% (Active only when you build 21cover.pdf)
% Comment out or override below when building main
\makeatletter
\renewcommand{\thepage}{\alph{page}} % a,b,c,...
\setcounter{page}{1}
\makeatother

% --- 2️⃣ FRONTMATTER (TOC, list of figs/tables) CONTINUE PAGE LETTERS ---
% (Used in 22main.pdf before chapters)
% Example: continue from 'f' (= page 6), so next = 'g' (=7)
% You can leave it here; it will only take effect when called in Markdown.
\newcommand{\frontmatterpages}{
  \makeatletter
  \renewcommand{\thepage}{\alph{page}}
  \setcounter{page}{7}   % adjust based on last cover page
  \makeatother
}

% --- 3️⃣ RESTART NUMBERING FOR MAIN CHAPTERS (1,2,3,...) ---
\newcommand{\startmainpages}{
  \pagenumbering{arabic}
  \setcounter{page}{1}
}
% --- 4️⃣ CONTINUE NUMBERING FOR MAIN CHAPTERS ---
\newcommand{\continuemainpages}{
  \pagenumbering{arabic}   % use Arabic numbers
  % do NOT reset page counter; continues from previous section
}